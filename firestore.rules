rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: abierto. Antes de producción: autenticación y validación de campos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
